interface:
  display_name: "Implementation Briefing"
  short_description: "Brief a substantial completed implementation"
  default_prompt: "Use $explain-implementation to build a standalone, source-backed briefing; use Lavish only when explicitly requested."
