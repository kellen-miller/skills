interface:
  display_name: "Grill Plan Build"
  short_description: "Orchestrate bounded reviews and human briefing"
  default_prompt: "Use $grill-plan-build to route this effort, orchestrate its fixed review lifecycle, and finish with a local interactive implementation briefing."
