interface:
  display_name: "Grill Plan Build"
  short_description: "Plan centrally, build in parallel, review results"
  default_prompt: "Use $grill-plan-build to resolve the design, plan in the main context, implement with smaller agents in separate worktrees, then validate, independently review, and explain the integrated result."
