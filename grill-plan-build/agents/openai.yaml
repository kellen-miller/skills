interface:
  display_name: "Grill Plan Build"
  short_description: "Plan visually, build, review, and transfer ownership"
  default_prompt: "Use $grill-plan-build to route this effort, run its local human plan review and fixed review lifecycle, then finish with a local interactive implementation briefing."
