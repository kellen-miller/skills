interface:
  display_name: "Grill Plan Build"
  short_description: "Plan, build, review, and transfer ownership"
  default_prompt: "Use $grill-plan-build to route this effort through its fixed review lifecycle, then finish with a local interactive implementation briefing."
